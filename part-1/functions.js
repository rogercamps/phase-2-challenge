// filterBetween
function filterBetween(array, min, max) {
  let array = []
  minVal = min
  maxVal = max

  let newArr = arr.filter(min, max)
}

// numProps
function numProps(obj) {
  let obj = {}
  let objLength = Object.keys(friend)
  objLength.length

}

module.exports = {function}
